import React from 'react';

export default function SmokeTest() {
  return (
    <div className="max-w-full text-center bg-sky-300 text-white font-extrabold p-3">
      TAILWIND SMOKE CHECK — styles should be visible (bg-sky-300)
    </div>
  );
}
